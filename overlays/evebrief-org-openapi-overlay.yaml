overlay: 1.0.0
info:
  title: API Evangelist enhancements for the onchain-risk-oracle API
  version: 1.0.0
extends: ../openapi/evebrief-org-openapi.json
x-generated: '2026-09-19'
x-method: generated
x-source: >-
  Generated from openapi/evebrief-org-openapi.json plus the probed artifacts in this repo (agent card, x402
  manifest, live 402 responses). Captures what the FastAPI-generated contract leaves out without mutating
  the provider's file. Every value below was observed or read from the provider's own documents; nothing is
  proposed that the host does not do.
actions:
- target: $
  description: The contract declares no servers[]; the host is the one the card, the x402 manifest and the fetch URL agree on.
  update:
    servers:
    - url: https://oracle.evebrief.org
      description: Observed host (source https://oracle.evebrief.org/openapi.json; agent card url https://oracle.evebrief.org/)
- target: $.info
  description: Link the provider's other machine-readable surfaces and record the payment contract.
  update:
    description: >-
      On-chain risk check for EVM addresses (eth, base, bsc, arb, op, polygon) against a curated rug-pull
      feed, returning a none/low/high/critical verdict with labels, reasons, evidence and sources. Exposed as
      an A2A 0.3.0 JSON-RPC agent; every task is paid via x402.
    x-agent-card: https://oracle.evebrief.org/.well-known/agent-card.json
    x-a2a-endpoint: https://oracle.evebrief.org/
    x-x402-manifest: https://oracle.evebrief.org/.well-known/x402
    x-payment:
      protocol: x402
      version: 2
      scheme: exact
      asset: RLUSD
      network: 'xrpl:0 (XRP Ledger mainnet)'
      amount: '0.01'
      pay_to: rH2tcNh56xoLUssRubra7DSDCgrReSZodW
      facilitator: https://xrpl-facilitator-mainnet.t54.ai
      challenge_status: 402
      max_timeout_seconds: 600
- target: $.paths['/'].post
  description: The JSON-RPC task endpoint. Add the observed 402, the input contract from the card's skill inputSchema, and tags.
  update:
    description: >-
      A2A JSON-RPC 2.0 endpoint for the onchain-risk-check skill. Unpaid calls (any body) return 402 with
      x402 PaymentRequirements in the PAYMENT-REQUIRED header and the body; retry with PAYMENT-SIGNATURE.
    tags: [Risk Check]
    x-a2a-skill: onchain-risk-check
    x-input-contract:
      source: a2a/evebrief-org-agent-card.json#/skills/0/inputSchema
      type: object
      required: [address]
      properties:
        address: {type: string, pattern: '^0x[0-9a-fA-F]{40}$'}
        chain: {type: string, enum: [eth, base, bsc, arb, op, polygon]}
        deep: {type: boolean, default: false}
        fork: {type: boolean, default: false}
    x-output-contract:
      source: a2a/evebrief-org-agent-card.json#/skills/0/outputSchema
      required: [address, risk, labels, reasons, evidence, sources]
      risk_enum: [none, low, high, critical]
    responses:
      '402':
        description: Payment required — x402 v2 PaymentRequirements (observed 2026-09-19 on GET and POST without PAYMENT-SIGNATURE).
        headers:
          PAYMENT-REQUIRED: {description: base64-encoded JSON copy of the PaymentRequirements body, schema: {type: string}}
        content:
          application/json:
            schema:
              type: object
              properties:
                x402Version: {type: integer, const: 2}
                resource: {type: object}
                accepts: {type: array, items: {type: object}}
                error: {type: string}
- target: $.paths['/.well-known/agent.json'].get
  update: {tags: [Discovery], x-a2a-path: legacy}
- target: $.paths['/.well-known/agent-card.json'].get
  update: {tags: [Discovery], x-a2a-path: canonical}
- target: $.paths['/.well-known/x402'].get
  update: {tags: [Discovery], x-standard: x402 v2 resource-server manifest}
- target: $.paths['/healthz'].get
  update:
    tags: [Health]
    description: 'Observed: {"status":"ok","payment_mode":"xrpl","feed_records":114110,"feed_latest_ts":"2026-09-19T23:20:48+00:00"}'
- target: $
  update:
    tags:
    - {name: Risk Check, description: The paid onchain-risk-check task}
    - {name: Discovery, description: Agent card and x402 manifest}
    - {name: Health, description: Free health endpoint}
